windows:
  - name: "TEMPERATURES"
    clear: true
    root: "~/McGill_RoboSub_2014"
    layout: even-horizontal
    panes:
      - cmd: "clear && rostopic echo /status/temperature"
      - cmd: "clear && rostopic echo /electrical_interface/temperature"
      - cmd: "clear && rostopic echo /electrical_interface/temperature1"
      - cmd: "clear && rostopic echo /electrical_interface/temperature2"
      - cmd: "clear && rostopic echo /electrical_interface/temperature3"
      - cmd: "clear && rostopic echo /electrical_interface/temperature4"
      - cmd: "clear && rostopic echo /electrical_interface/temperature5"
